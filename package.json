{
  "name": "template",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.html",
  "scripts": {
    "start": "parcel serve ./src/index.html --no-cache",
    "build": "rm -rf dist; parcel build ./src/index.html --no-cache --no-source-maps --public-url .; rm -rf .parcel-cache;",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^9.8.6",
    "cssnano": "^4.1.10",
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.10"
  }
}
